export enum EditMode {
  MEETINGS = 'meetings',
  AVAILABILITY = 'availability',
  GROUPS = 'groups',
  DETAILS = 'details',
  TYPES = 'types',
  CALENDARS = 'calendars',
  NOTIFICATIONS = 'notifications',
  GATES = 'gates',
  SIGNOUT = 'signout',
}

export enum Intents {
  JOIN = 'join',
}
