export enum EditMode {
  MEETINGS = 'meetings',
  AVAILABILITY = 'availability',
  DETAILS = 'details',
  TYPES = 'types',
  CALENDARS = 'calendars',
  NOTIFICATIONS = 'notifications',
  SIGNOUT = 'signout',
}
