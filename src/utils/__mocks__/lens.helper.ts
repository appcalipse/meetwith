import { LensProfile } from '../lens.helper'

const getLensHandlesForAddress = async (
  address: string
): Promise<LensProfile[] | undefined> => {
  return undefined
}

const getLensProfile = async (
  handle: string
): Promise<LensProfile | undefined> => {
  return undefined
}

export default {
  getLensHandlesForAddress,
  getLensProfile,
}
